import React from 'react';
import s from './../Setting/Setting.module.css';

const Setting = (props) =>{
    return(
        <div className={s.setting}>
            <h1>Setting</h1>
        </div>
    );
}

export default Setting;